import React from 'react';
import { StyleSheet, Text, View, Button } from 'react-native';

class Flexbox extends React.Component {
    
    static navigationOptions = {
        title: 'Flex box',
      };

    render() {
        return (<Text>Flex box</Text>);
    }
}

export default Flexbox;
